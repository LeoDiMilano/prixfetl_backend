class DataPreprocessing:
    @staticmethod
    def create_features(df: pd.DataFrame) -> pd.DataFrame:
        """
        Rajoute les colonnes de variations hebdo, mensuelles, etc.
        """
        pass

    @staticmethod
    def handle_missing_values(df: pd.DataFrame) -> pd.DataFrame:
        """
        Gère les NaN, par ffill, bfill ou suppression
        """
        pass
